---
import Layout from "../../layouts/Layout.astro";
import ModalGallery from "../../components/ui/ModalGallery.astro";
import Container from "../../components/Container.astro";
import Headline from "../../components/Headline.astro";
import { Headlines, NAME2 } from "../../consts";
import GalleryTab from "../../components/GalleryTab.astro";
import { Image } from "astro:assets";
import imagePathsPortrait from "../../js/imagePaths/imagePathsPortrait.js";
let pageName = "Portraits";
---

<Layout title={`${NAME2} - ${pageName}`}>
  <!--Modal -->
  <ModalGallery data="Portrait" />
  <Container>
    <Headline Heading={Headlines[5].heading} Subtitle={Headlines[5].subtitle} />
    <GalleryTab />
    <div class="gap-9" style="
  columns: 3 300px;
  column-gap: 1rem;">
      {
        imagePathsPortrait.map((image, i) => (
          <button class="grid-item mb-4" id={i.toString()}>
            <Image
              class="rounded-md  image-item4 grid place-content-center"
              src={image}
              alt={image}
              width={400}
              height={200}
              format="webp"
              quality="low"
            />
          </button>
        ))
      }
    </div>
    <!-- 
    <div class="masonGrid3 w-full">
      <div class="masonCol"></div>
      <div class="masonCol"></div>
      <div class="masonCol"></div>
    </div>-->
  </Container>
</Layout>
<!-- 
<script>
  import { createGrid } from "../../js/gridGallery.js";
  import imagePathsPortrait from "../../js/imagePaths/imagePathsPortrait.js";

  imagePathsPortrait.forEach((path, i) => {
    createGrid(path, i);
  });
</script>-->
